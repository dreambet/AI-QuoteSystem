# 英维克成本分析计算公式总结

## 一、来源与范围

- 来源文件：`D:\Desktop\英维克成本分析260728(1).xls-副本.xls`
- 工作表：`Sheet1`
- 共识别 5 个产品区块、91 个计算公式单元格，归纳为 12 类公式模板。

## 二、行号定义

以每个产品的首行为 `r`，上一行为费率参数行 `h`：

| 产品 | 物料编码 | 产品首行 `r` | 费率参数行 `h` |
|---|---:|---:|---:|
| 产品 1 | 35763000072 | 4 | 3 |
| 产品 2 | 35763000076 | 26 | 25 |
| 产品 3 | 35763000078 | 47 | 46 |
| 产品 4 | 35763000080 | 68 | 67 |
| 产品 5 | 35763000088 | 88 | 87 |

其中：

- `H`：材料规格数据；`K`：材料成本。
- `O`：工费率；`P`：加工时长；`Q`：单制程成本。
- `R`：机加工成本；`S`：管销；`T`：小计；`U`：利润；`V/W`：含税成本或样品价格。
- `S_h`：管销费率；`U_h`：利润率。

## 三、通用计算公式

### 1. 材料成本

```excel
K_r = SUM(H_{r+7}*H_{r+8})
```

例如：`K4=SUM(H11*H12)`。

### 2. 单制程成本

对有工费率和加工时长的工序行 `i`：

```excel
Q_i = O_i/60.0*P_i
```

`60.0` 用于将加工分钟数换算为小时。

### 3. 机加工成本

```excel
R_r = SUM(Q_r:Q_{r+8})
```

### 4. 材料损耗、刀具损耗等附加成本

```excel
Q_{r+9}  = R_r*N_{r+9}
Q_{r+10} = R_r*N_{r+10}
```

### 5. 管销

原表按各附加费用行逐项相加：

```excel
S_r = (R_r
     + Q_{r+9} + Q_{r+10} + Q_{r+11} + Q_{r+12}
     + Q_{r+13} + Q_{r+14} + Q_{r+15} + Q_{r+16})*S_h
```

### 6. 小计

```excel
T_r = K_r + R_r + S_r
    + Q_{r+9} + Q_{r+10} + Q_{r+11} + Q_{r+12}
    + Q_{r+13} + Q_{r+14} + Q_{r+15} + Q_{r+16}
```

### 7. 利润

```excel
U_r = T_r*U_h
```

### 8. 含税合计成本（产品 1～4）

```excel
V_r = (T_r+U_r)*1.13
```

`1.13` 表示在小计和利润基础上加计 13% 税费。

### 9. 样品价格（产品 1～4）

```excel
W_r = V_r*2.0
```

## 四、特殊公式

### 产品 2～4的特殊工序费用

产品 2～4分别存在以下公式：

```excel
Q39 = 15.0*J33
Q60 = 15.0*J54
Q81 = 15.0*J75
```

按产品首行 `r` 表示为：

```excel
Q_{r+13} = 15.0*J_{r+7}
```

### 产品 5的结构差异

产品 5没有 `V88` 公式，直接在 `W88` 计算含税合计成本：

```excel
W88 = (T88+U88)*1.13
```

因此，产品 5 的 `W` 列对应含税合计成本，不再执行产品 1～4 的 `V*2.0` 样品价格公式。

## 五、产品 1 的完整示例

```excel
K4 = SUM(H11*H12)
Q4:Q12 = O_i/60.0*P_i
R4 = SUM(Q4:Q12)
Q13 = R4*N13
Q14 = R4*N14
S4 = (R4+Q13+Q14+Q15+Q16+Q17+Q18+Q19+Q20)*S3
T4 = K4+R4+S4+Q13+Q14+Q15+Q16+Q17+Q18+Q19+Q20
U4 = T4*U3
V4 = (T4+U4)*1.13
W4 = V4*2.0
```

## 六、总体计算顺序

```text
材料规格
   ↓
材料成本 K
   ↓
各工序单制程成本 Q
   ↓
机加工成本 R
   ↓
损耗、刀具及其他附加费用 Q
   ↓
管销 S
   ↓
小计 T
   ↓
利润 U
   ↓
含税合计成本 V 或 W
   ↓
样品价格 W（产品 1～4）
```

> 注意：并非所有工序行都使用公式；当加工时长或相关输入为空时，原表部分单元格可能保持为 0 或空值。
